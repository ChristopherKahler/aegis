---
phase: 08-end-to-end-validation
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified: [README.md]
autonomous: false
---

<objective>
## Goal
Audit the README.md section-by-section against the 90 delivered spec files, identify every factual discrepancy introduced by 7 phases of drift since Phase 1, and surgically rewrite only what's wrong — preserving the README's narrative voice and quality as a "sales document" that makes engineers want to star the repo.

## Purpose
The README was written in Phase 1 (2026-02-12/13). Seven phases of content creation happened since. It is the primary public-facing document for AEGIS and must be 100% accurate against the delivered v0.1.0 specification set before any release activity.

## Output
- README.md with all factual claims verified against source files
- Every number, name, ID, count, and structural claim confirmed or corrected
- Missing concepts added where the v0.1 reality has outgrown the Phase 1 README (e.g., version-locking, validation)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/08-end-to-end-validation/08-01-SUMMARY.md — validation results, cross-ref fixes, version-lock manifest
@.paul/HANDOFF-readme-alignment.md — drift risks catalogued, subagent strategy defined

## Source Files (verification targets)
@README.md — THE file being audited and updated
@docs/validation/validation-summary.md — v0.1 inventory (90 files, exact counts per component type)
@docs/validation/version-manifest.yaml — complete file inventory with SHA-256 hashes

## Verification Sources (read by subagents for fact extraction)
@src/domains/ — 14 domain files (owner_agents, tool_affinities, section structure)
@src/schemas/ — 5 shared schema files
@src/transform/schemas/ — 4 transform schema files
@src/rules/ — 3 shared rule files
@src/transform/rules/ — 2 transform rule files
@src/tools/ — 8 tool adapter files
@src/core/personas/ — 12 core persona files
@src/transform/personas/ — 5 transform persona files
@src/core/agents/ — 12 core agent assembly manifests
@src/transform/agents/ — 5 transform agent assembly manifests
@src/core/workflows/ — 8 core workflow files
@src/transform/workflows/ — 4 transform workflow files
@src/core/commands/ — 4 core command files
@src/transform/commands/ — 4 transform command files
@docs/standards/ — convention specification files
</context>

<acceptance_criteria>

## AC-1: Factual Accuracy — All Claims Verified
```gherkin
Given the README makes claims about agent names, domain counts, tool names, schema structures, workflow phases, and command names
When each claim is compared against the corresponding source spec file
Then every claim matches the source file exactly (names, IDs, counts, structures)
```

## AC-2: Structural Completeness — No Missing Concepts
```gherkin
Given v0.1 delivered concepts not present when README was written (version-locking, validation, Transform execution details, command UX patterns)
When the README is reviewed for completeness
Then all significant v0.1 concepts are represented (added surgically, not restructured)
```

## AC-3: Narrative Preservation — Voice and Quality Maintained
```gherkin
Given the README serves as the primary "sales document" for AEGIS
When sections are corrected or added
Then the README's distinctive voice, narrative quality, and compelling structure are preserved or enhanced
```

## AC-4: No Fabrication — Zero Hallucinated Content
```gherkin
Given every README claim must trace to a source file
When the updated README is reviewed
Then no claim exists that cannot be verified against a delivered spec file
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extract verified facts from all source spec files</name>
  <files>All 90 spec files (read-only)</files>
  <action>
    Deploy targeted subagents to READ specific file sets and return EXACT values. The main agent must already understand the full system architecture before deploying subagents — subagents are used ONLY for batch fact extraction, never for summarization, interpretation, or content generation.

    **CRITICAL CONSTRAINT:** The main agent has already read the README, PROJECT.md, ROADMAP.md, validation-summary.md, and HANDOFF. It understands the two-system architecture, the 14 domains, the agent team model, the epistemic schema, the three output layers, intervention levels, the transformation model, and the PAUL integration. Subagents are extracting raw data, not building understanding.

    **Subagent deployment strategy (parallel where possible):**

    Subagent A — Domain facts:
    - Read all 14 domain files in src/domains/
    - Return EXACT: id, number, name, owner_agents from each file's frontmatter
    - Return: tool_affinities primary tools per domain

    Subagent B — Persona facts:
    - Read all 12 core persona files in src/core/personas/
    - Read all 5 transform persona files in src/transform/personas/
    - Return EXACT: id, name, role from each file's frontmatter

    Subagent C — Agent + workflow + command facts:
    - Read all 17 agent files (src/core/agents/ + src/transform/agents/)
    - Return EXACT: id, persona_ref, domains[] from each
    - Read all 12 workflow files (src/core/workflows/ + src/transform/workflows/)
    - Return EXACT: id, name, phase from each
    - Read all 8 command files (src/core/commands/ + src/transform/commands/)
    - Return EXACT: id, name, command_name from each

    Subagent D — Schema + rule + tool facts:
    - Read all 9 schema files (src/schemas/ + src/transform/schemas/)
    - Return EXACT: id, name from each
    - Read all 5 rule files (src/rules/ + src/transform/rules/)
    - Return EXACT: id, name, priority from each
    - Read all 8 tool files (src/tools/)
    - Return EXACT: id, name, domains_served from each

    **What to avoid and WHY:**
    - Do NOT ask subagents to "summarize" or "describe" anything — this is where hallucination enters
    - Do NOT ask subagents to compare against the README — that's the main agent's job
    - Do NOT ask subagents to suggest changes — they extract facts, period
    - Each subagent returns structured data (lists of exact values), not prose
  </action>
  <verify>Main agent receives structured fact lists from all 4 subagents covering all 90 spec files. Spot-check 3-5 values by reading source files directly.</verify>
  <done>AC-4 satisfied: Verified fact base established from source files for all 90 specs</done>
</task>

<task type="auto">
  <name>Task 2: Section-by-section README audit and surgical rewrite</name>
  <files>README.md</files>
  <action>
    The MAIN AGENT (not subagents) performs this task entirely. Using the verified facts from Task 1, walk through every README section and compare claims against source data.

    **Section audit checklist (README order):**

    1. **Identity section** — Name, acronym expansion. Likely accurate (established Phase 1, unchanged).

    2. **What AEGIS Is** — "multi-session, multi-agent" claim. Verify agent count claim.

    3. **Core Philosophy** — Principles. Likely accurate (philosophical, not factual claims about files).

    4. **The 14 Audit Domains (Domains 0-13)** — For each domain:
       - Verify domain name matches source file
       - Verify owner agent name matches source file's owner_agents field
       - Verify "What to audit" items are reasonable (these are high-level, unlikely to drift)
       - Check: does README use display names or IDs? Match against delivered files.

    5. **The Agent Team (12 agents listed 1-12)** — For each agent:
       - Verify persona name matches source persona file
       - Verify domain assignments match source agent file's domains[]
       - Verify role descriptions are consistent with persona specs
       - Check: README says "12 agent personas" — is this still accurate for Core? (Yes: 12 Core + 5 Transform = 17 total)

    6. **Execution Phases (0-5)** — For each Core phase:
       - Verify agent assignments match workflow specs
       - Verify phase descriptions match workflow specs
       - Verify input/output claims match workflow specs

    7. **The Three Output Layers** — Layer A/B/C descriptions. Check phase references in the pipeline diagram (README says "Phases 0-5 / Phases 6-7 / Phase 8").

    8. **AEGIS Transform** — Verify 5 agent count, agent names, "Diagnosis is decentralized" principle.

    9. **Intervention Levels** — 4 levels table. Verify against src/transform/rules/intervention-levels.md or relevant spec.

    10. **The Transformation Model** — 4-layer model. Verify against src/transform/schemas/.

    11. **Transform Agent Team (5 agents)** — Verify names, roles, what they consume/produce against transform persona and agent specs.

    12. **Transform Execution Phases (6-8)** — Verify phase numbers, agent assignments, inputs/outputs against transform workflow specs. Critical: README says Phases 6/7/8 for Transform. Do the workflow specs use these phase numbers or different ones?

    13. **Change Risk in Remediation** — 4 risk dimensions. Verify against change-risk schema.

    14. **Safety & Liability Framework** — Conservative bias, confidence thresholds, unsafe context flagging. Verify against transform safety rules.

    15. **PAUL Integration** — What gets generated, phased remediation, verification gates. Verify against execution-plan schema.

    16. **Pattern Corpus & Feedback Loop** — Aspirational/conceptual. Low drift risk.

    17. **The Formal Epistemic Schema** — 7-layer stack, confidence modeling, hygiene rules. Verify layer names/content against src/schemas/finding.md and src/schemas/confidence.md.

    18. **Disagreement Resolution System** — Resolution models, disagreement structure, protocol. Verify against src/schemas/disagreement.md and src/rules/disagreement-rules.md.

    19. **Disagreement Visualization Model** — 5 axes, canonical views. Verify against schemas.

    20. **Reality Gap Framework** — 5 RG domains, agent outputs. Verify against reality-gap-analyst persona/agent.

    21. **Language-Specific Failure Models** — Per-language audit questions. Low drift risk (informational).

    22. **Tooling Stack** — Tool names, what they do, domains served, cost. Verify tool names and domain assignments against src/tools/ specs. Critical: "Syft + Grype" presented as combined — now separate files.

    23. **Output Format** — Report sections. Verify against src/schemas/report.md.

    24. **Signal Categories** — 6 dimensions. Verify against src/schemas/signal.md.

    25. **Related Reading** — Verify reference file path exists.

    **Rewrite rules:**
    - Fix only what's factually wrong. Do not rewrite sections that are accurate.
    - Preserve the README's distinctive voice (mythological framing, confident assertions, rhetorical structure).
    - When adding new content (e.g., version-locking section), match the existing voice.
    - Add a brief "v0.1 Status" section near the top indicating this is a validated specification set.
    - Update any counts, names, IDs, or structural claims that have drifted.
    - If the README says "Phases 6-8" for Transform but the workflow specs use different identifiers, align to whichever is correct.
  </action>
  <verify>
    After rewrite, perform a final pass reading the updated README and confirming:
    - Every agent name matches a persona file ID or name
    - Every domain name/number matches a domain file
    - Every tool name matches a tool file
    - Every schema/rule reference matches a source file
    - All counts (14 domains, 12 core agents, 5 transform agents, 8 tools, etc.) match the validation inventory
    - No new claims introduced that aren't sourced from spec files
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: All README claims verified, missing concepts added, voice preserved</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>README.md surgically updated to align with v0.1.0 delivered specifications</what-built>
  <how-to-verify>
    1. Read the updated README.md
    2. Verify the narrative voice is preserved (compelling, not clinical)
    3. Verify any new sections (version-locking, status) feel organic
    4. Confirm no "AI-ified" language crept in (hedging, over-qualification)
    5. Spot-check 3-5 specific claims against source files if desired
    6. Confirm: "Would this README make an engineer want to star this repo?"
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- All 90 spec files in src/ — these are version-locked at v0.1.0
- docs/validation/ — validation artifacts are final
- docs/standards/ — convention specs are final
- .paul/ project management files (except STATE.md update)
- reference/ — original design conversation preserved as-is

## SCOPE LIMITS
- Only modify README.md — no other files
- Do not restructure the README's section order (preserve existing navigation)
- Do not add aspirational/future features — only document what v0.1 actually delivered
- Do not expand language-specific failure models or tooling details beyond what specs define
- Do not create new documentation files (no CHANGELOG, no separate docs)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Every domain name, number, and owner in README matches src/domains/ frontmatter
- [ ] Every agent/persona name in README matches src/core/personas/ and src/transform/personas/ frontmatter
- [ ] Every tool name and domain assignment in README matches src/tools/ frontmatter
- [ ] All workflow phase numbers and agent assignments match src/core/workflows/ and src/transform/workflows/
- [ ] All command names match src/core/commands/ and src/transform/commands/
- [ ] Component counts match validation-summary.md inventory (90 files, 14 domains, 17 agents, etc.)
- [ ] No claim in README references a file, concept, or structure that doesn't exist in the delivered specs
- [ ] README voice is compelling and consistent throughout
- [ ] User has approved the final README via checkpoint
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- README.md is factually accurate against all 90 v0.1.0 spec files
- Narrative quality preserved or enhanced
- User has explicitly approved the updated README
</success_criteria>

<output>
After completion, create `.paul/phases/08-end-to-end-validation/08-02-SUMMARY.md`
</output>
